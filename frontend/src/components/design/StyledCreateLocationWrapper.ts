import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StyledCreateLocationWrapper = styled(Box)`
  min-width: 400px;
  text-align: center;
  margin: 36px;
`;
