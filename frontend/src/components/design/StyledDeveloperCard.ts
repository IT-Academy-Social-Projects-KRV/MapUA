import { Card } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StyledDeveloperCard = styled(Card)(() => ({
  maxHeight: '600px',
  borderRadius: '15px',
  maxWidth: '200px',
  marginBottom: '50px',
  marginTop: '30px'
}));
